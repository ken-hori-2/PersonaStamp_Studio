# ============================================================================
# Fish Audio SDK (app/app.py用)
# ============================================================================
fish-audio-sdk>=2025.6.3

# ============================================================================
# 環境変数管理
# ============================================================================
python-dotenv>=1.0.0

# ============================================================================
# Web UI (Streamlit)
# ============================================================================
streamlit>=1.28.0

# ============================================================================
# 音声認識（文字起こし）- app/app.py用
# ============================================================================
openai-whisper>=20231117  # Whisper音声認識モデル

# ============================================================================
# 音源分離 - app_audio_tools/app.py用
# ============================================================================
demucs>=4.0.0  # 高精度音源分離
torchcodec>=0.8.0  # demucsの依存関係（一部のモデルで必要）

# ============================================================================
# YouTube音声ダウンロード - app_audio_tools/app.py用
# ============================================================================
yt-dlp>=2023.0.0  # YouTubeダウンローダー

# ============================================================================
# PyTorch関連（Whisperとdemucsの依存関係）
# ============================================================================
# Streamlit Cloud用にCPU版を指定
torch>=2.0.0
torchaudio>=2.0.0  # demucsの依存関係として必要

# ============================================================================
# 数値計算（Whisperとdemucsの依存関係として必要）
# ============================================================================
numpy>=1.24.0

# ============================================================================
# demucsの依存関係（明示的に記載）
# ============================================================================
einops>=0.6.0  # demucsの依存関係
julius>=0.2.7  # demucsの依存関係
lameenc>=1.2.0  # demucsの依存関係
pyyaml>=6.0  # demucsの依存関係
tqdm>=4.65.0  # demucsの依存関係

# ============================================================================
# 注意事項
# ============================================================================
# ffmpegはシステムレベルでインストールが必要です
# - macOS: brew install ffmpeg
# - Ubuntu/Debian: sudo apt install ffmpeg
# - Windows: https://ffmpeg.org/download.html からダウンロード
# Streamlit Cloudでは packages.txt に記載してください
